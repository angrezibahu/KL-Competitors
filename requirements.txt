# Daily competitor capture pipeline
playwright==1.49.1       # headless browser: rendered HTML + screenshots (1.49 supports Ubuntu 24.04 deps)
beautifulsoup4==4.12.3   # HTML parsing for SEO + structured data
lxml==6.1.0              # fast parser backend for BeautifulSoup (security: XXE fix + CVE-2026-41066)
Pillow==12.2.0           # dominant-colour extraction from screenshots (security release: CVE fixes)
pytrends==4.9.2          # Google Trends (UNOFFICIAL api, best-effort only)
anthropic>=0.49.0        # AI-overview visibility (scraper/aio.py); only needed for the weekly AIO job
